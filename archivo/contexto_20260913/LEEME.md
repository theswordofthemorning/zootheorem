# Archived context — 2026-09-13

Literal snapshots from before the entry documents were shortened.
They are historical sources, not current instructions. Start with
[the current plan](../../PLAN_INVESTIGACION.md).

`fuentes.json` records original paths, sizes and SHA-256 hashes.
Search headings in `indice.tsv`, then read only the required lines of
the corresponding `.txt`. Original relative links still refer to the
directory of the source file recorded in the manifest.

Do not load this archive at session start or append current work here.
Keep the plan short and link new detailed notes from it.
