%I #10 Sep 17 2026 16:35:58
%S 1,3,7,15,27,45,73,111,166,237,330,451,605,797,1033,1316,1655,2054,2517,3051,3665
%N Number of new Collatz merging classes at level n with exactly 8 mixed-parity steps.
%C Let T be the Collatz map (A014682): T(x) = x/2 for even x and (3*x+1)/2 for odd x. Iterate T simultaneously from r and r+1, for each integer r with 0 <= r < 2^n.
%C Count r if the two trajectories first meet after exactly n iterations, have the same number of odd inputs among these n iterations, and have opposite input parities at exactly 8 of them. An iteration with opposite input parities is called a mixed-parity step; steps are numbered 1 through n, including the meeting step.
%C The first nonzero term is at n = 9. The sequences A399819, A399820 and A399821 count the cases with 8, 10 and 12 mixed-parity steps, respectively.
%C The equal odd counts ensure that the meeting holds for the entire residue class modulo 2^n: if each path has s odd inputs, replacing r by r + 2^n*m adds 3^s*m to both values at step n. Thus each counted r represents an infinite family of consecutive pairs, not just one numerical coincidence.
%C These sequences refine A398793 by the number of mixed-parity steps. If e_j(n) denotes the same count with j mixed-parity steps, then sum_j e_j(n) = A398793(n-1) = A398792(n) - 2*A398792(n-1), for n >= 2. Only even j can occur, since equal odd counts require equally many mixed steps of each orientation.
%C This decomposition records how different parity patterns contribute to the density of merging consecutive pairs, in connection with the question studied by Gao. The quantity e_j(n)/2^n is the density added at level n by that part of the count. Proving lim A398792(n)/2^n = 1 remains an open objective; the three fixed values j = 8, 10, 12 alone do not settle it.
%C The counts require only finite trajectories and do not assume the Collatz conjecture. For lifted pairs r + 2^n*m, r + 2^n*m + 1 with m >= 1, equal finite total stopping times follow if their common trajectory reaches 1.
%C A concrete connection with earlier work is the pair (3067,3068), used by Elia and Tucker to disprove Garner's conjecture about equal-height pairs. Under T it first meets at 692 after 18 iterations, with 10 odd inputs on each path and 8 mixed-parity steps. Hence r = 3067 is one of the 237 classes counted by a(18).
%D G.-G. Gao, On consecutive numbers of the same height in the Collatz problem, Discrete Mathematics 112 (1993), 261-267.
%H Omar Said, <a href="https://github.com/theswordofthemorning/zootheorem">Statements, proofs, sequence data and verification scripts</a>
%H Marcus Elia and Amanda Tucker, <a href="https://arxiv.org/abs/1511.09141">Consecutive Integers and the Collatz Conjecture</a>, Integers 15 (2015), A54, Section 4.
%e a(9) = 1, corresponding to r = 65 modulo 512. The trajectories 65, 98, 49, 74, 37, 56, 28, 14, 7, 11 and 66, 33, 50, 25, 38, 19, 29, 44, 22, 11 first meet after 9 iterations of T. Each has four odd inputs, and their inputs have opposite parity at steps 1 through 7 and at step 9.
%e a(10) = 3: the three classes are r = 131, 193, 473 modulo 1024. For r = 131, the two trajectories first meet at 11 after 10 iterations; each has four odd inputs, with opposite parity at steps 1, 2, 3, 5, 6, 7, 8, 10.
%o (Python)
%o def step(x):
%o     return (3*x + 1)//2 if x & 1 else x//2
%o def ok(r, n):
%o     x, y, sx, sy, m = r, r+1, 0, 0, 0
%o     for t in range(1, n+1):
%o         m += ((x ^ y) & 1)
%o         sx += x & 1; sy += y & 1
%o         x, y = step(x), step(y)
%o         if x == y:
%o             return t == n and sx == sy and m == 8
%o     return False
%o def a(n):
%o     return sum(ok(r, n) for r in range(2**n - 1))
%Y Cf. A398792, A398793, A399820, A399821.
%K nonn,more,changed
%O 9,2
%A _Omar Said_, Sep 12 2026
